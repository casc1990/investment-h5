# Cloudflare Pages Functions Configuration
name = "investment-h5"
compatibility_date = "2024-01-01"

# Cron Trigger: 每个交易日 21:00 北京时间 (UTC 13:00, 周一至周五)
# Cloudflare Dashboard 里还需手动创建 CRON TRIGGER 并关联到这个脚本
[triggers]
crons = ["0 13 * * 1-5"]

# Pages Functions 目录
pages_build_output_dir = "dist"
